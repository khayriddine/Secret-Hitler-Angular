export enum Status {
    Normal, President, Chancellor, Dead
}
export enum Role{
    Spectator, Liberal, Fascist, Hitler
}
export enum VoteValue {
    Yes, No
}
export enum NotifType{
    Normal,Investigate,ChoosePresident,KillPlayer
}
export enum CardType{
    Fascist, Liberal
}
export enum CardsAction{
    Discard, Peek
}